package plausibcheck.adress

import com.zbin.drools.model.Address;
import com.zbin.drools.model.fact.AddressCheckResult;

rule "Postcode 6 numbers"

    when
    then
		System.out.println("logs in rule temp : check success!");
end